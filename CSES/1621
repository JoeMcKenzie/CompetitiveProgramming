#include <bits/stdc++.h>
using namespace std;

int main() {
	int n{ };
	set<int> s;
	std::cin >> n;
	for (int i = 0; i < n; ++i) {
		int num{ };
		std::cin >> num;
		s.insert(num);
	}
	std::cout << s.size();
}
